Add the raw data like pcap files to /data/raw
Add a processed data like csv to /data/processed/features
Trained model's will be saved at /data/processed/models

Install the requirements from the requirements.txt
Use streamlit for the dashboard UI
